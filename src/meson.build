# Define the executable
executable('verceptron',
  sources: [
    'verceptron.vala',
    'perceptron.vala',
    'multilayer_perceptron.vala',
  ],
  dependencies: [
    glib_dep,
    gobject_dep,
  ],
  install: true,
)